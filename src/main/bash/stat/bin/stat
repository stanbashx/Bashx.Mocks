#!/usr/local/bin/bash

if [[ -n "${MOCKS_STAT_EXIT_CODE}" ]]; then
 if [[ "${MOCKS_STAT_EXIT_CODE}" =~ ^[1-9][0-9]*$ && ${MOCKS_STAT_EXIT_CODE} -lt 256 ]]; then
  exit ${MOCKS_STAT_EXIT_CODE}
 else
  echo 'Wrong exit code!' >&2; exit 1
 fi
fi

printf '%s' "${MOCKS_STAT_SIZE}"
