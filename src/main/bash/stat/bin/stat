#!/usr/local/bin/bash

if [[ "${MOCKS_STAT_EXIT_CODE}" =~ ^[1-9][0-9]*$ ]]; then
 exit ${MOCKS_STAT_EXIT_CODE}
elif [[ -n "${MOCKS_STAT_EXIT_CODE}" ]]; then
 echo 'Wrong exit code!' >&2; exit 1
fi

printf '%s' "${MOCKS_STAT_SIZE}"
